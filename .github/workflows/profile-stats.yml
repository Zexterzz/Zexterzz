name: Atualizar Estatísticas do Perfil

on:
  workflow_dispatch:

jobs:
  stats:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4

      - name: Criar pasta profile
        run: mkdir -p profile

      - name: Gerar estatísticas
        run: |
          curl -L "https://github-readme-stats.vercel.app/api?username=Zexterzz&show_icons=true&theme=tokyonight&hide_border=true" \
          -o profile/stats.svg

      - name: Gerar linguagens
        run: |
          curl -L "https://github-readme-stats.vercel.app/api/top-langs/?username=Zexterzz&layout=compact&langs_count=8&theme=tokyonight&hide_border=true" \
          -o profile/top-langs.svg

      - name: Salvar arquivos
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add profile/

          git commit -m "Atualizar estatisticas do perfil" || exit 0

          git push

